apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: production
spec:
  selector:
    app: frontend-production
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
